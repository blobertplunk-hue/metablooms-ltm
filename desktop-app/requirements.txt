# MetaBlooms Sync - Dependencies
# Core app works without any dependencies (uses built-in tkinter)

# Optional: For system tray support (minimize to tray, notifications)
pystray>=0.19.0
Pillow>=9.0.0

# Optional: For prettier notifications on Windows
# win10toast>=0.9
